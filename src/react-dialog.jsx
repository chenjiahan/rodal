var React = require('react');
require('./react-dialog.scss');

var Dialog = React.createClass({
    render: function() {
        return (
            <div className="react-dialog">
                dialog
            </div>
        )
    }
})

module.exports = Dialog;